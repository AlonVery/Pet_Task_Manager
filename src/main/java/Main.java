import web.application.ApplicationBootstrap;import web.application.config.ApplicationConfig;import web.application.config.Environment;public class Main {    public static void main(String[] args) {        /*        Что планируется - Реализовать фильтр и пагинацию на запрос GET /users (апи для дефолтной админ панели по зарегистрированным пользователям)        Что сделал - добавил в Request декомпозиции path на Map<String, String> как Query параметры        Остается:            1. Как то доработать хэндлер - или общий (чтоб для каждого запроса искал параметры) или только для GetAllUsersController так,        чтоб передавал разобранные из реквеста квери в комманду, а та уже в юзкейс который работает с базой - заметка в GetAllUsersUseCase            2. Как то реализовать передачу pathParam с хендлера в юзкейс для поиска по этому параметру            2. Реализовать поиск по параметру в БД            3. Реализовать фильтр по квери параметрам которые придут с        */        ApplicationConfig config =                new ApplicationConfig(8080, Environment.DEV);        new ApplicationBootstrap().start(config);    }}